version: '3.3'
services:
  app:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0
    ports:
      - "4999:8000"
    volumes:
        - /mnt/disk2/patron:/app/app/qr
        - /mnt/disk0/images:/app/app/pai